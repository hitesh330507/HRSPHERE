server:
  port: 0  # Random port for tests

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/auth_db_test
    user: test
    password: test
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379
      password: testpass

jwt:
  secret: dGVzdC1zZWNyZXQta2V5LWZvci10ZXN0aW5nLW9ubHktMjU2Yml0cw==
  access-token-expiry-ms: 5000
  refresh-token-expiry-ms: 30000

logging:
  level:
    com.hrsphere: DEBUG
    org.springframework.security: DEBUG
